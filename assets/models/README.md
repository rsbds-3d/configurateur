# Modèles 3D

Placez ici les fichiers `.glb` ou `.gltf` exportés depuis Blender, FreeCAD ou OpenCascade.

Le viewer accepte aussi un chargement manuel via l'interface. Les fichiers STEP/STP doivent être convertis avant usage web.
